package com.example.order.domain.types;

import com.example.order.domain.entity.Account;
import com.example.order.types.Money;

public class AuditMessage {
    public AuditMessage(Account sourceAccount, Account targetAccount, Money targetMoney) {

    }
}
